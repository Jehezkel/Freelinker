import { IntegrationToken } from './integration-token.model';

describe('IntegrationToken', () => {
  it('should create an instance', () => {
    expect(new IntegrationToken()).toBeTruthy();
  });
});
